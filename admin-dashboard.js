(()=>{
const $=id=>document.getElementById(id);
let current='dashboard',dashboardLoaded=false,bookingsLoaded=false,tradeinsLoaded=false,financeLoaded=false;
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
function wrap(el,page){if(!el||el.closest('.admin-page'))return;const s=document.createElement('section');s.className='admin-page';s.dataset.page=page;el.before(s);s.appendChild(el);}
function show(requested){const admin=$('adminView');if(!admin)return;const page=requested==='settings'?'finance':requested;if(!['dashboard','inventory','bookings','finance'].includes(page))return;current=requested;admin.querySelectorAll('.admin-page').forEach(s=>s.hidden=s.dataset.page!==page);admin.querySelectorAll('.admin-page-nav button').forEach(b=>b.classList.toggle('active',b.dataset.page===requested));if(admin.classList.contains('hidden'))return;if(page==='dashboard'&&!dashboardLoaded){dashboardLoaded=true;loadDashboard();}if(page==='bookings'){if(!bookingsLoaded){bookingsLoaded=true;window.loadBookings?.();}if(!tradeinsLoaded){tradeinsLoaded=true;window.loadTradeins?.();}}if(page==='finance'&&!financeLoaded){financeLoaded=true;window.loadFinanceSettings?.();}}
function init(){const admin=$('adminView');if(!admin||admin.dataset.pagesReady)return;admin.dataset.pagesReady='1';const top=admin.querySelector('.topbar'),stats=admin.querySelector('.stats-row'),nav=document.createElement('nav');nav.className='admin-page-nav';['Dashboard','Inventory','Bookings','Finance','Settings'].forEach(name=>{const b=document.createElement('button');b.type='button';b.textContent=name;b.dataset.page=name.toLowerCase();nav.appendChild(b);});top.after(nav);const dash=document.createElement('section');dash.className='admin-page';dash.dataset.page='dashboard';dash.innerHTML='<div class="dashboard-hero"><span class="eyebrow">DASHBOARD</span><h3>Dealership overview</h3><p class="muted">A quick summary of inventory and customer activity.</p></div><div class="dashboard-stats"><div class="stat-card"><span>Available for sale</span><strong id="dashAvailable">0</strong></div><div class="stat-card"><span>Sold vehicles</span><strong id="dashSold">0</strong></div><div class="stat-card"><span>Pending / On hold</span><strong id="dashPending">0</strong></div><div class="stat-card"><span>Inventory value</span><strong id="dashValue">$0</strong></div><div class="stat-card"><span>Upcoming test drives</span><strong id="dashUpcomingBookings">0</strong></div><div class="stat-card"><span>New requests</span><strong id="dashNewBookings">0</strong></div></div><section class="panel dashboard-bookings"><div class="panel-head"><div><span class="eyebrow">TEST DRIVES</span><h3>Requests by appointment date</h3></div></div><div id="dashRecentBookings" class="bookings-list"><div class="muted">Loading bookings…</div></div></section>';nav.after(dash);wrap(admin.querySelector('.layout-grid'),'inventory');wrap(admin.querySelector('.inventory-panel'),'inventory');wrap(admin.querySelector('.bookings-panel'),'bookings');wrap(admin.querySelector('.tradeins-panel'),'bookings');wrap(admin.querySelector('.finance-panel'),'finance');if(stats)stats.remove();nav.addEventListener('click',e=>{const b=e.target.closest('button[data-page]');if(b)show(b.dataset.page);});show('dashboard');}
function dateLabel(value){if(!value)return'No appointment date';const d=new Date(`${value}T12:00:00`);return Number.isNaN(d)?String(value):d.toLocaleDateString(undefined,{weekday:'long',year:'numeric',month:'long',day:'numeric'});}
function dateKey(value){if(!value)return'9999-99-99';return String(value).slice(0,10);}
function bookingTime(b){return String(b.preferred_time||'').trim()||'Time not specified';}
function renderBookings(bookings){const list=$('dashRecentBookings');if(!list)return;if(!bookings.length){list.innerHTML='<div class="booking-empty">No test drive bookings yet.</div>';return;}const groups=new Map();bookings.forEach(b=>{const key=dateKey(b.preferred_date);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(b);});list.innerHTML=[...groups.entries()].sort((a,b)=>a[0].localeCompare(b[0])).map(([date,items])=>`<section class="dashboard-date-group"><div class="dashboard-date-heading"><h4>${esc(dateLabel(date==='9999-99-99'?'':date))}</h4><span>${items.length} request${items.length===1?'':'s'}</span></div>${items.sort((a,b)=>bookingTime(a).localeCompare(bookingTime(b))).map(b=>{const v=b.Vehicles?`${b.Vehicles.Year||''} ${b.Vehicles.Make||''} ${b.Vehicles.Model||''}`.trim():(b.vehicle_name||'Vehicle not specified');return `<article class="booking-card"><div class="booking-main"><h4>${esc(b.first_name)} ${esc(b.last_name)}</h4><div class="booking-details"><div><span>Vehicle</span><strong>${esc(v)}</strong></div><div><span>Appointment</span><strong>${esc(bookingTime(b))}</strong></div><div><span>Status</span><strong>${esc(b.status||'New')}</strong></div></div></div></article>`}).join('')}</section>`).join('');}
async function loadDashboard(){const a=$('dashAvailable');if(!a||typeof db==='undefined')return;try{const {data:vs,error:ve}=await db.from('Vehicles').select('id,Status,Price');if(ve)throw ve;const rows=vs||[];const status=v=>String(v.Status||'Available').toLowerCase();const available=rows.filter(v=>status(v)==='available');a.textContent=available.length;$('dashSold').textContent=rows.filter(v=>status(v)==='sold').length;$('dashPending').textContent=rows.filter(v=>['pending','hold','on hold'].includes(status(v))).length;$('dashValue').textContent=new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(available.reduce((sum,v)=>sum+(Number(v.Price)||0),0));const {data:bs,error:be}=await db.from('test_drive_bookings').select('*,Vehicles(Year,Make,Model)').order('preferred_date',{ascending:true});if(be)throw be;const bookings=bs||[];const today=new Date();today.setHours(0,0,0,0);const todayKey=today.toISOString().slice(0,10);$('dashUpcomingBookings').textContent=bookings.filter(b=>dateKey(b.preferred_date)>=todayKey&&String(b.status||'').toLowerCase()!=='cancelled').length;$('dashNewBookings').textContent=bookings.filter(b=>String(b.status||'New').toLowerCase()==='new').length;renderBookings(bookings);}catch(error){console.error(error);const list=$('dashRecentBookings');if(list)list.innerHTML='<div class="booking-empty">Could not load dashboard data.</div>';}}
function hookAuth(){const previous=window.showAdmin;if(typeof previous!=='function'||previous.__dashboardHooked)return;let lastSession=null,busy=false;function wrapped(session){const key=session?.access_token||session?.user?.id||'';if(busy||key===lastSession)return;busy=true;lastSession=key;try{previous(session);}finally{busy=false;}dashboardLoaded=false;bookingsLoaded=false;tradeinsLoaded=false;financeLoaded=false;show(current);}wrapped.__dashboardHooked=true;window.showAdmin=wrapped;const oldAuth=window.showAuth;if(typeof oldAuth==='function')window.showAuth=function(){lastSession=null;dashboardLoaded=false;bookingsLoaded=false;tradeinsLoaded=false;financeLoaded=false;return oldAuth();};}
document.addEventListener('DOMContentLoaded',()=>{init();hookAuth();});
})();