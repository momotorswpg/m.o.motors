const U="https://dpsgtliddmdvfwjahkkq.supabase.co",K="sb_publishable_f-MRqpvq-FGsxQ7dBNIyKQ_r8MB1VM0",H={apikey:K,Authorization:`Bearer ${K}`,"Content-Type":"application/json"};
const sel=document.getElementById("vehicle"),dateInput=document.getElementById("date"),timeSelect=document.getElementById("time"),msg=document.getElementById("formMessage"),form=document.getElementById("testDriveForm"),params=new URLSearchParams(location.search);
const slots=[];for(let h=10;h<18;h++)for(const m of [0,30]){if(h===17&&m===30)continue;const d=new Date(2000,0,1,h,m);slots.push(d.toLocaleTimeString("en-US",{hour:"numeric",minute:"2-digit"}));}
const localDateISO=()=>{const d=new Date();return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().split("T")[0]};
dateInput.min=localDateISO();
function resetTimes(text="Select a vehicle and date first"){timeSelect.innerHTML=`<option value="">${text}</option>`;timeSelect.disabled=true;}
function slotIsPast(slot,date){if(date!==localDateISO())return false;const now=new Date(),match=slot.match(/^(\d{1,2}):(\d{2}) (AM|PM)$/i);if(!match)return false;let h=Number(match[1]),m=Number(match[2]);if(match[3].toUpperCase()==="PM"&&h!==12)h+=12;if(match[3].toUpperCase()==="AM"&&h===12)h=0;const slotDate=new Date(now.getFullYear(),now.getMonth(),now.getDate(),h,m,0,0);return slotDate<=now;}
function renderTimes(booked=[],date=dateInput.value){timeSelect.innerHTML='<option value="">Select a time</option>';slots.forEach(slot=>{const unavailable=booked.includes(slot),past=slotIsPast(slot,date),o=document.createElement("option");o.value=slot;o.disabled=unavailable||past;o.textContent=unavailable?`${slot} — Unavailable`:past?`${slot} — Past`:slot;timeSelect.appendChild(o)});timeSelect.disabled=false;}
async function loadAvailability(){const date=dateInput.value,vehicleId=sel.value;if(!vehicleId){resetTimes("Select a vehicle first");return}if(!date){resetTimes();return}if(new Date(`${date}T12:00:00`).getDay()===0){resetTimes("Closed Sundays");msg.textContent="M.O Motors is closed on Sundays. Please choose Monday through Saturday.";return}msg.textContent="Checking available times…";resetTimes("Loading available times…");try{const r=await fetch(`${U}/rest/v1/rpc/get_test_drive_booked_times`,{method:"POST",headers:H,body:JSON.stringify({p_vehicle_id:Number(vehicleId),p_date:date})});if(!r.ok)throw new Error();const data=await r.json();renderTimes(data.map(row=>row.preferred_time),date);msg.textContent="Choose an available 30-minute appointment time for this vehicle."}catch{renderTimes([],date);msg.textContent="We couldn't check existing bookings, so please choose a time and we'll confirm availability."}}
fetch(`${U}/rest/v1/Vehicles?select=*&order=created_at.desc`,{headers:H}).then(r=>r.json()).then(rows=>{rows.filter(v=>String(v.Status||"Available").toLowerCase()==="available").forEach(v=>{const o=document.createElement("option");o.value=v.id;o.textContent=`${v.Year} ${v.Make} ${v.Model}`;if(params.get("vehicle")===String(v.id))o.selected=true;sel.appendChild(o)});if(sel.value&&dateInput.value)loadAvailability()}).catch(()=>msg.textContent="We couldn't load the vehicle list. Please call us to book your test drive.");
dateInput.addEventListener("change",loadAvailability);
sel.addEventListener("change",loadAvailability);
form.addEventListener("submit",async e=>{e.preventDefault();const payload={vehicle_id:Number(sel.value),vehicle_name:sel.options[sel.selectedIndex]?.textContent||null,preferred_date:dateInput.value,preferred_time:timeSelect.value,first_name:document.getElementById("firstName").value.trim(),last_name:document.getElementById("lastName").value.trim(),phone:document.getElementById("phone").value.trim(),email:document.getElementById("email").value.trim()};if(!payload.preferred_time){msg.textContent="Please choose an available appointment time.";return}const button=form.querySelector('button[type="submit"]');button.disabled=true;msg.textContent="Booking your test drive…";try{const r=await fetch(`${U}/rest/v1/test_drive_bookings`,{method:"POST",headers:{...H,Prefer:"return=minimal"},body:JSON.stringify(payload)});if(r.status===409){await loadAvailability();throw new Error("That time was just booked for this vehicle. Please choose another available time.")}if(!r.ok){let detail="Unable to save booking";try{const body=await r.json();detail=body?.message||body?.error?.message||body?.hint||detail}catch{}throw new Error(detail)}const notify=await fetch(`${U}/functions/v1/lead-notification`,{method:"POST",headers:H,body:JSON.stringify({type:"test_drive",data:payload})});if(!notify.ok)console.warn("Booking saved but email notification failed");msg.textContent=notify.ok?"You're booked! A confirmation email has been sent to you with your appointment details.":"You're booked! Your request was saved, but the confirmation email could not be sent. We'll follow up with you.";form.reset();dateInput.min=localDateISO();resetTimes()}catch(err){msg.textContent=err.message||"We couldn't submit your request. Please try again or call M.O Motors."}finally{button.disabled=false}});
