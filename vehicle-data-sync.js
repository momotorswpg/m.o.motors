(()=>{const URL='https://dpsgtliddmdvfwjahkkq.supabase.co',KEY='sb_publishable_f-MRqpvq-FGsxQ7dBNIyKQ_r8MB1VM0',id=new URLSearchParams(location.search).get('id'),root=document.getElementById('vehicleDetail');if(!root||!id)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));const money=v=>Number.isFinite(Number(v))?new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(Number(v)):'$—';async function get(path){const r=await fetch(`${URL}/rest/v1/${path}`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`}});if(!r.ok)throw new Error(await r.text());return r.json()}async function render(){try{const [rows,photos]=await Promise.all([get(`Vehicles?select=*&id=eq.${encodeURIComponent(id)}`),get(`vehicle_images?select=*&vehicle_id=eq.${encodeURIComponent(id)}&order=is_primary.desc,sort_order.asc`).catch(()=>[])]);const v=rows[0];if(!v)throw new Error('Vehicle not found');const title=`${v.Year||''} ${v.Make||''} ${v.Model||''}`.trim(),sold=String(v.Status||'').toLowerCase()==='sold',carfax=v.CarfaxURL||'',features=v.AdditionalInfo||'',description=v.Description||'';const specs=[['Year',v.Year],['Make',v.Make],['Model',v.Model],['Trim',v.Trim],['Odometer',v.Mileage!==null&&v.Mileage!==undefined&&v.Mileage!==''?`${Number(v.Mileage).toLocaleString('en-CA')} km`:null],['Body Style',v.BodyStyle],['Transmission',v.Transmission],['Engine Cylinders',v.EngineCylinders],['Engine Size',v.EngineSize?`${v.EngineSize} L`:null],['Drivetrain',v.Drivetrain],['Exterior Colour',v.ExteriorColor],['Interior Colour',v.InteriorColor],['Doors',v.Doors],['Fuel Type',v.FuelType],['Passengers',v.Passengers],['VIN',v.VIN]].filter(([,x])=>x!==null&&x!==undefined&&x!=='');document.title=`${title} | M.O Motors`;root.innerHTML=`<div class="vehicle-detail-grid"><section class="detail-gallery"><div class="detail-main-image ${photos.length?'':'placeholder'}">${photos.length?`<img id="syncedMainVehiclePhoto" src="${esc(photos[0].image_url)}" alt="${esc(title)}">`:'<span>PHOTO COMING SOON</span>'}${sold?'<div class="sold-badge">SOLD</div>':''}${photos.length>1?'<button class="gallery-arrow gallery-prev" type="button">‹</button><button class="gallery-arrow gallery-next" type="button">›</button>':''}</div>${photos.length>1?`<div class="detail-thumbs">${photos.map((p,i)=>`<button class="detail-thumb ${i===0?'active':''}" type="button" data-photo="${i}"><img src="${esc(p.image_url)}" alt="${esc(title)} photo ${i+1}"></button>`).join('')}</div>`:''}</section><section class="detail-info"><p class="detail-year">${sold?'SOLD':'PRE-OWNED'}</p><h1 class="detail-title">${esc(title)}</h1><div class="detail-meta"><span>${v.Mileage?Number(v.Mileage).toLocaleString('en-CA'):'—'} km</span><span>${esc(v.Transmission||'—')}</span></div><div class="detail-price"><strong>${money(v.Price)}</strong></div>${sold?'<span class="sold-label">This vehicle has been sold</span>':`<div class="vehicle-action-stack"><div class="primary-action-row"><a class="btn btn-primary" href="book-test-drive.html?vehicle=${encodeURIComponent(v.id)}">Book a Test Drive</a><a class="btn detail-outline" href="pre-approval.html?vehicle=${encodeURIComponent(v.id)}">Get Pre-Approved</a></div></div>`}${carfax?`<a class="btn carfax-btn" href="${esc(carfax)}" target="_blank" rel="noopener">View CARFAX Report ↗</a>`:''}${features?`<div class="detail-description"><h2>Features &amp; Additional Information</h2><p>${esc(features).replace(/\n/g,'<br>')}</p></div>`:''}${description?`<div class="detail-description"><h2>Description</h2><p>${esc(description).replace(/\n/g,'<br>')}</p></div>`:''}<div class="detail-description"><h2>Vehicle Details</h2></div><div class="spec-grid">${specs.map(([l,x])=>`<div><span>${esc(l)}</span><b>${esc(x)}</b></div>`).join('')}</div></section></div>`;if(photos.length>1){let current=0;const show=n=>{current=(n+photos.length)%photos.length;const img=document.getElementById('syncedMainVehiclePhoto');if(img)img.src=photos[current].image_url;root.querySelectorAll('[data-photo]').forEach((b,i)=>b.classList.toggle('active',i===current))};root.querySelector('.gallery-prev')?.addEventListener('click',()=>show(current-1));root.querySelector('.gallery-next')?.addEventListener('click',()=>show(current+1));root.querySelectorAll('[data-photo]').forEach(b=>b.addEventListener('click',()=>show(Number(b.dataset.photo))))}}catch(e){console.error('Vehicle data sync failed',e);if(!root.children.length||/Loading vehicle/i.test(root.textContent))root.innerHTML='<div class="inventory-empty">We could not load this vehicle right now. Please try again.</div>'}}setTimeout(render,100)})();