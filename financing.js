(()=>{const $=s=>document.querySelector(s);const money=n=>new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(isFinite(n)?n:0);function calc(){const price=+$('#calcPrice').value||0,down=+$('#calcDown').value||0,trade=+$('#calcTrade').value||0,term=+$('#calcTerm').value||84,apr=(+$('#calcApr').value||0)/100,freq=$('#calcFrequency').value;const taxable=$('#calcTax').checked;const principal=Math.max(0,(price*(taxable?1.12:1))-down-trade);const monthlyRate=apr/12;const monthly=monthlyRate?principal*monthlyRate/(1-Math.pow(1+monthlyRate,-term)):principal/term;const periods=freq==='weekly'?52:freq==='biweekly'?26:12;const payment=monthly*12/periods;const total=payment*periods*(term/12);$('#paymentOut').textContent=money(payment);$('#borrowOut').textContent=money(Math.max(0,total-principal));$('#obligationOut').textContent=money(total);$('#principalOut').textContent=money(principal)}document.querySelectorAll('[data-calc]').forEach(el=>el.addEventListener('input',calc));$('#calcFrequency').addEventListener('change',calc);$('#calcTax').addEventListener('change',calc);calc();const form=$('#financeForm');form.addEventListener('submit',e=>{e.preventDefault();if(!$('#creditConsent').checked){$('#formMessage').textContent='Please confirm your consent before submitting a financing application.';return}$('#formMessage').textContent='Your application is ready to be securely submitted. The database submission step will be connected next; do not enter a SIN until that secure workflow is enabled.'})})();